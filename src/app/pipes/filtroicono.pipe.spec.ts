import { FiltroiconoPipe } from './filtroicono.pipe';

describe('FiltroiconoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroiconoPipe();
    expect(pipe).toBeTruthy();
  });
});
